# 모델 학습 과정 정리

이 프로젝트의 모델 학습은 실내 보행 내비게이션에서 수집한 IMU CSV 로그를 이용해 걸음 여부와 보폭을 추정하는 JSON 모델을 만드는 과정이다. 현재 저장소에는 최신 흐름인 self-supervised 모델과, 이전 방식인 supervised / pseudo-labeled 모델이 함께 들어 있다.

## 1. 전체 구조

| 구분 | 파일 | 역할 |
| --- | --- | --- |
| 최신 학습 실행 | `train_self_supervised.ps1` | `data` 폴더의 CSV 로그로 self-supervised 모델을 학습하고 APK 빌드까지 실행 |
| 최신 학습 코드 | `tools/train_self_supervised_step_model.py` | 라벨이 없는 IMU 로그에서 후보 걸음 구간을 찾고, 클러스터링으로 걸음 패턴을 학습 |
| 최신 모델 산출물 | `models/self_supervised_step_model.json` | 앱에서 사용하는 self-supervised 걸음/보폭 모델 |
| 이전 학습 실행 | `train_models.ps1` | 분류 모델과 보폭 회귀 모델을 각각 학습 |
| 이전 분류 모델 | `tools/train_step_classifier.py` | 걸음 여부를 logistic regression으로 학습 |
| 이전 보폭 모델 | `tools/train_step_length_model.py` | 보폭을 linear regression으로 학습 |
| 이전 모델 산출물 | `models/step_classifier.json`, `models/step_length_model.json` | 앱에서 로드 가능한 기존 모델 |

Flutter 앱에서는 `pubspec.yaml`의 asset 설정을 통해 아래 모델 JSON을 포함한다.

```yaml
assets:
  - models/step_classifier.json
  - models/step_length_model.json
  - models/self_supervised_step_model.json
```

## 2. 학습 데이터 수집

학습 데이터는 앱의 실내 내비게이션 화면에서 내보낸 IMU 로그 CSV이다. 기본 입력 위치는 프로젝트 루트의 `data` 폴더이며, 현재 이 폴더에는 `imu_log_*.csv` 파일들이 들어 있다.

CSV에는 다음 계열의 값들이 포함된다.

- 시간과 위치: `timestamp`, `position_x`, `position_y`, `distance_m`
- 방향 정보: `heading_radians`, `heading_degrees`, `heading_label`
- 가속도/자이로/자기장: `accel_*`, `user_accel_*`, `gyro_*`, `mag_*`
- 회전 벡터 기반 방향: `geomagnetic_rotation_azimuth`, `game_rotation_azimuth`
- 걸음 감지 관련 값: `filtered_accel`, `active_threshold`, `crossings`, `steps`
- 모델 학습용 확장 값: `step_source`, `step_confidence`, `seconds_since_prev_step`, `recent_step_interval_mean`, `filtered_to_user_accel_ratio`, `accel_to_gyro_ratio`
- 기존 supervised 학습용 라벨: `manual_step_label`, `true_step_label`, `imu_step_detected`
- 보폭 타깃: `manual_step_length_m`, `last_step_length_m`

수동 라벨이 있으면 `manual_step_label` 또는 `true_step_label`을 우선 사용한다. 없으면 기존 앱의 `imu_step_detected` 값을 fallback 라벨로 사용한다. Self-supervised 방식은 수동 라벨 없이도 동작한다.

## 3. 최신 방식: Self-Supervised 모델 학습

최신 흐름은 `train_self_supervised.ps1`을 실행하는 것이다.

```powershell
.\train_self_supervised.ps1
```

기본값은 다음과 같다.

- 입력: `data`
- 출력: `models/self_supervised_step_model.json`
- 학습 epoch: `3600`
- 학습 후 `flutter analyze` 실행
- 마지막에 `build_apk.ps1`로 APK 빌드 실행

입력 폴더나 epoch을 바꾸려면 다음처럼 실행한다.

```powershell
.\train_self_supervised.ps1 -InputDir "data" -Output "models\self_supervised_step_model.json" -Epochs 3600
```

분석 단계를 건너뛰려면 다음 옵션을 붙인다.

```powershell
.\train_self_supervised.ps1 -SkipAnalyze
```

### 3.1 후보 걸음 구간 추출

`tools/train_self_supervised_step_model.py`는 각 CSV의 `filtered_accel` 값을 읽고 peak를 찾는다. 이때 너무 낮은 값, 주변보다 뚜렷하지 않은 값, 너무 가까운 peak는 후보에서 제외한다.

주요 기준은 다음과 같다.

- 최근 window 평균과 표준편차로 baseline 계산
- `filtered_accel`이 baseline보다 충분히 큰지 확인
- 앞뒤 값보다 큰 local peak인지 확인
- 후보 간 최소 간격을 둬서 중복 peak 제거

이렇게 얻은 후보 index마다 하나의 학습 sample을 만든다.

### 3.2 특징 벡터 생성

각 후보 sample은 21개 feature로 변환된다.

```text
user_accel_magnitude
gyro_magnitude
tilt_gyro_magnitude
filtered_accel
active_threshold
threshold_margin
threshold_margin_ratio
heading_change_degrees
angular_velocity_dps
window_mean_user_accel
window_std_user_accel
window_mean_gyro
window_std_gyro
window_max_heading_change
seconds_since_prev_step
heading_change_since_prev_step
recent_step_interval_mean
recent_step_interval_std
heading_change_rate_since_prev_step
filtered_to_user_accel_ratio
accel_to_gyro_ratio
```

이 feature들은 보행 강도, 흔들림, 회전량, 이전 걸음과의 시간 간격, 최근 cadence 등을 함께 반영한다.

### 3.3 정규화

모든 후보 sample에 대해 feature별 평균과 표준편차를 계산한다. 이후 각 feature는 아래 방식으로 정규화된다.

```text
normalized = (value - mean) / std
```

이 평균과 표준편차는 모델 JSON의 `normalization.means`, `normalization.stds`에 저장된다. 앱에서도 같은 값으로 입력 feature를 정규화한다.

### 3.4 K-means 클러스터링

정규화된 후보 sample들을 k-means로 묶는다. 기본 cluster 수는 3이다.

```powershell
py tools\train_self_supervised_step_model.py --input-dir data --output models\self_supervised_step_model.json --clusters 3
```

각 cluster는 다음 정보를 기준으로 점수를 받는다.

- 평균 peak 높이
- 평균 prominence
- cadence 일관성
- 평균 cadence가 일반적인 보행 간격에 가까운 정도
- gyro 값이 너무 큰 경우의 penalty

점수가 가장 높은 cluster가 `step_cluster_index`로 선택된다. 이 cluster가 실제 걸음에 가장 가까운 prototype으로 사용된다.

### 3.5 걸음 확률과 threshold 결정

각 후보 sample은 선택된 step cluster의 centroid와 얼마나 가까운지에 따라 step probability를 얻는다. threshold는 전체 확률 분포의 평균과 표준편차를 이용해 자동으로 정해지고, `0.45`에서 `0.8` 사이로 제한된다.

모델 JSON에는 다음 값으로 저장된다.

- `clusters`: cluster centroid와 통계값
- `step_cluster_index`: 걸음 cluster 번호
- `step_decision_threshold`: 걸음으로 판단할 확률 기준

앱에서는 `SelfSupervisedStepModel.classifyStep()`이 이 threshold를 사용한다.

### 3.6 Pseudo 보폭 생성

Self-supervised 방식은 수동 보폭 라벨 없이도 보폭 모델을 만들기 위해 pseudo target을 만든다. 보폭 pseudo target은 아래 요소들을 조합한다.

- cadence가 빠른 정도
- peak 높이
- peak prominence
- step probability

결과 보폭은 기본적으로 `0.5m`에서 `0.95m` 사이로 제한된다.

### 3.7 보폭 회귀 학습

정규화 feature를 입력으로 하고 pseudo 보폭을 target으로 하는 선형 회귀를 학습한다. 기본 epoch은 `3600`이며, 학습 결과는 아래 값으로 저장된다.

- `length_weights`
- `length_bias`
- `minimum_step_length_meters`
- `maximum_step_length_meters`
- `metrics.length_mae`
- `metrics.length_rmse`

앱에서는 `SelfSupervisedStepModel.predictStepLengthMeters()`가 이 값을 사용해 보폭을 추정한다.

## 4. 이전 방식: 분류 모델 + 보폭 모델 학습

이전 방식은 두 모델을 따로 학습한다.

```powershell
.\train_models.ps1
```

기본 출력은 다음과 같다.

- `models/step_classifier.json`
- `models/step_length_model.json`

### 4.1 걸음 분류 모델

직접 실행하려면 다음 명령을 사용한다.

```powershell
py tools\train_step_classifier.py --input-dir data --output models\step_classifier.json
```

이 스크립트는 CSV 파일 단위로 train / validation을 나누고, 21개 feature를 정규화한 뒤 weighted logistic regression을 학습한다.

라벨 우선순위는 다음과 같다.

1. 지정한 `--label-column`
2. `manual_step_label`
3. `true_step_label`
4. `imu_step_detected`

주요 옵션은 다음과 같다.

```powershell
py tools\train_step_classifier.py `
  --input-dir data `
  --output models\step_classifier.json `
  --label-column manual_step_label `
  --target-specificity 0.7 `
  --epochs 1800 `
  --learning-rate 0.035
```

학습 후 validation set에서 threshold를 고른다. 이때 `target-specificity`를 만족하는 방향으로 threshold를 선택한다.

### 4.2 보폭 회귀 모델

직접 실행하려면 다음 명령을 사용한다.

```powershell
py tools\train_step_length_model.py --input-dir data --output models\step_length_model.json
```

이 스크립트는 `manual_step_length_m`을 우선 target으로 사용하고, 없으면 `last_step_length_m`을 사용한다. target이 없거나 0 이하인 row는 학습에서 제외한다.

주요 옵션은 다음과 같다.

```powershell
py tools\train_step_length_model.py `
  --input-dir data `
  --output models\step_length_model.json `
  --target-column manual_step_length_m `
  --epochs 1600 `
  --learning-rate 0.03 `
  --min-length 0.832 `
  --max-length 0.95
```

모델은 linear regression SGD로 학습되며, 예측 보폭은 `min-length`와 `max-length` 사이로 clamp된다.

## 5. 산출 JSON 구조

### 5.1 `self_supervised_step_model.json`

주요 필드는 다음과 같다.

- `model_type`: 모델 종류
- `feature_names`: 입력 feature 순서
- `normalization`: feature 평균과 표준편차
- `clusters`: k-means centroid와 cluster 통계
- `step_cluster_index`: 걸음 cluster 번호
- `step_decision_threshold`: 걸음 판정 기준
- `length_weights`, `length_bias`: 보폭 회귀 계수
- `metrics`: 후보 window 수, pseudo 보폭 MAE/RMSE
- `training_summary`: 사용한 CSV 파일과 학습 요약

### 5.2 `step_classifier.json`

주요 필드는 다음과 같다.

- `weights`, `bias`: logistic regression 계수
- `decision_threshold`: 분류 threshold
- `normalization`: feature 평균과 표준편차
- `metrics.train`, `metrics.validation`: accuracy, precision, recall, specificity, balanced accuracy, F1
- `training_summary`: train/validation 파일과 sample 수

### 5.3 `step_length_model.json`

주요 필드는 다음과 같다.

- `weights`, `bias`: linear regression 계수
- `normalization`: feature 평균과 표준편차
- `minimum_step_length_meters`
- `maximum_step_length_meters`
- `metrics.train`, `metrics.validation`: MAE, RMSE, R2
- `training_summary`: train/validation 파일과 sample 수

## 6. 앱에서 모델을 사용하는 방식

Flutter 쪽 모델 로더는 `lib/services` 아래에 있다.

- `self_supervised_step_model.dart`
- `step_classifier_model.dart`
- `step_length_model.dart`

각 모델은 asset JSON을 읽고, 학습 때 저장한 정규화 값으로 feature를 정규화한 뒤 예측한다.

Self-supervised 모델의 사용 흐름은 다음과 같다.

1. 센서 값에서 21개 feature 생성
2. `normalization` 값으로 정규화
3. cluster centroid와의 거리로 step probability 계산
4. `step_decision_threshold` 이상이면 걸음으로 판단
5. `length_weights`, `length_bias`로 보폭 예측
6. 예측 보폭을 최소/최대 보폭 범위 안으로 제한

## 7. 검증 및 배포 흐름

모델을 새로 만든 뒤에는 다음 순서로 확인한다.

```powershell
flutter analyze
.\build_apk.ps1
```

`train_self_supervised.ps1` 또는 `train_models.ps1`을 사용하면 기본적으로 `flutter analyze`와 APK 빌드가 이어서 실행된다.

검증 시 확인할 값은 다음과 같다.

- 학습 스크립트가 출력한 candidate 수 또는 sample 수
- self-supervised 모델의 `length_mae`, `length_rmse`
- supervised 분류 모델의 validation F1, specificity, balanced accuracy
- 보폭 모델의 validation MAE, RMSE
- 실제 기기에서 걸음 수와 이동 거리 변화가 자연스러운지

## 8. 재학습할 때의 권장 순서

1. 실제 기기에서 여러 보행 패턴으로 IMU CSV를 수집한다.
2. CSV 파일을 `data` 폴더에 넣는다.
3. 라벨이 없으면 self-supervised 방식을 먼저 실행한다.

```powershell
.\train_self_supervised.ps1
```

4. 수동 라벨이나 실제 보폭 라벨이 있으면 이전 supervised 방식도 함께 비교한다.

```powershell
.\train_models.ps1
```

5. 생성된 JSON의 `metrics`를 확인한다.
6. APK를 설치해 실내 내비게이션 화면에서 걸음 수, heading, 이동 거리를 확인한다.
7. 결과가 흔들리면 CSV를 더 모으거나 threshold, epoch, min/max 보폭 옵션을 조정해 재학습한다.

## 9. 한 줄 요약

현재 프로젝트의 기본 학습 흐름은 `data` 폴더의 IMU 로그를 `train_self_supervised.ps1`로 학습해 `models/self_supervised_step_model.json`을 만들고, Flutter 앱이 이 JSON을 asset으로 읽어 걸음 여부와 보폭을 실시간 추정하는 구조이다.
